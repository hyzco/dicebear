module.exports = {
  purge: [],
  theme: {
    extend: {
      gridTemplateColumns: {
        parts: 'repeat(auto-fill, minmax(120px, 1fr))',
        colors: 'repeat(auto-fill, minmax(22px, 1fr))',
      },
      spacing: {
        full: '100%'
      }
    },
  },
  variants: {},
  plugins: [],
}
